// Esta biblioteca nos permite utilizar jsx
import React from 'react';

// Esta biblioteca nos permite representar nuestra aplicación dentro del codigo html
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('app'));
root.render(
  <h1>Hello, world!</h1>
);
